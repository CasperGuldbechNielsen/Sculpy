<Page
    x:Class="Sculpy.View.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sculpy.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="150"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <RelativePanel Grid.Row="0">

            <AutoSuggestBox x:Name="SearchBox" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"
                                VerticalAlignment="Center" Width="250" Margin="4,4,8,4" RelativePanel.AlignRightWithPanel="True"
                                QueryIcon="Find" PlaceholderText="enter a search term"/>

        </RelativePanel>
        <Frame Grid.Row="1" Name="MainFrame"></Frame>



    </Grid>
</Page>
