<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sculpy.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:viewModel="using:Sculpy.ViewModel"
    x:Class="Sculpy.View.MapView"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewModel:MapViewModel/>
    </Page.DataContext>


    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="settingsButton" Label="Settings"/>
            </CommandBar.SecondaryCommands>
            <AppBarButton x:Name="locationButton" Icon="Target" Label="Where am I?">
                <Interactivity:Interaction.Behaviors>
                    <Core:EventTriggerBehavior EventName="Click">
                        <Core:InvokeCommandAction Command="{Binding setLocationCommand, Mode=TwoWay}"/>
                    </Core:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>
            </AppBarButton>
            <AppBarSeparator/>
            <AppBarButton x:Name="syncButton" Icon="Sync" Label="Syncronize"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <maps:MapControl 
			x:Name="sculptureMap" 
			MapServiceToken="QeLz32DxSNyF19VtizMv~dRnLRZKAEMEEGxV-4y5QxQ~An-7hyW97IOfT6BPFS0eVjruAHc-kLJT25BBhmhFRQRybVs41Pzvncm3o2Or6NZa" 
			ZoomInteractionMode="GestureAndControl" 
			LandmarksVisible="True" 
			IsRightTapEnabled="true"
			ZoomLevel="14"
			Loaded="SculptureMap_OnLoaded"/>

    </Grid>
</Page>
